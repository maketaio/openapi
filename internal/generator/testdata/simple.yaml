openapi: 3.1.0
info:
  title: Simple
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
        name:
          type: string
        metadata:
          type: object
          additionalProperties:
            type: string
